# Basic Authentication
AuthType Basic
AuthName "Restricted Area - Shooting Guides"
AuthUserFile /home/yojitu/yojitu.com/shooting-guides/.htpasswd
Require valid-user

# noindex header (conditional - only if mod_headers is available)
<IfModule mod_headers.c>
    Header set X-Robots-Tag "noindex, nofollow"
</IfModule>

# Disable directory listing
Options -Indexes

# Error Documents
ErrorDocument 401 "Authentication Required"
ErrorDocument 403 "Access Forbidden"
