# Allow members.json for JavaScript reading
<Files "members.json">
    Order allow,deny
    Allow from all
    Satisfy Any
</Files>

# Deny all other files (CSV, backup files, etc.)
<FilesMatch "^(?!members\.json$).*">
    Order deny,allow
    Deny from all
</FilesMatch>
