# ディレクトリリスティング無効化
Options -Indexes

# デフォルトドキュメント（静的版を優先）
DirectoryIndex index.html index.php

# エラーページ
ErrorDocument 403 /chatgpt-learning-platform/public/index.html
ErrorDocument 404 /chatgpt-learning-platform/public/index.html

# HTTPS強制（本番環境）
# <IfModule mod_rewrite.c>
#     RewriteEngine On
#     RewriteCond %{HTTPS} off
#     RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
# </IfModule>
